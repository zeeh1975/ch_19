const chatModel = require("./chat.model");
const productosModel = require("./productos.model");
const usuariosModel = require("./usuarios.model");

module.exports = { chatModel, productosModel, usuariosModel };
