const infoPageController = require("./infoPage.controller");
const productController = require("./product.controller");
const randomsController = require("./randoms.controller");
const sessionController = require("./session.controller");
const testProductController = require("./testproduct.controller");
const userController = require("./user.controller");
const webController = require("./web.controller");

module.exports = {
  infoPageController,
  productController,
  randomsController,
  sessionController,
  testProductController,
  userController,
  webController,
};
